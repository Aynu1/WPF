<Page x:Class="Mdam.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Mdam"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login">

    <Grid>
        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                BorderThickness="1" CornerRadius="10" Padding="10 5 10 5" BorderBrush="Silver">
            <StackPanel>
                <Label >Логин</Label>
                <TextBox x:Name="Logintb"></TextBox>
                <Label >Пароль</Label>
                <PasswordBox x:Name="Passtb"></PasswordBox>
                <StackPanel Orientation="Horizontal" Margin="0 5 0 0" HorizontalAlignment="Center">
                    <Button x:Name="Loginbtn" Click="Loginbtn_Click" Content="Вход" Margin="0 0 10 0"/>
                    <Button x:Name="Registrationbtn" Click="Registrationbtn_Click" Content="Регистрация"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
