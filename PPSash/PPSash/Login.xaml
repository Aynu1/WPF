<Page x:Class="PPSash.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PPSash"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login">

    <Grid>
        <Border CornerRadius="10" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="10 5 10 5" BorderThickness="1" BorderBrush="Silver" Background="Silver">
            <StackPanel>
                <Label >Логин</Label>
                <TextBox x:Name="Logintb"></TextBox>
                <Label >Пароль</Label>
                <PasswordBox x:Name="Passtb"></PasswordBox>
                <StackPanel Orientation="Horizontal" Margin="0 5 0 0" HorizontalAlignment="Center">
                    <Button x:Name="Loginbtn" Click="Loginbtn_Click" Content="Вход" Margin="0 0 10 0" Width="50"/>
                    <Button x:Name="Registrationbtn" Click="Registrationbtn_Click" Content="Регистрация" Width="75"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
